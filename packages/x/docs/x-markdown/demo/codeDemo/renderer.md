## zh-CN

使用 `renderer` 做渲染前的效果处理将 html 渲染成文字，同时给每个标题前加上 `🚀`。

## en-US

Use `renderer` to perform pre-rendering effect processing and render HTML as text, while adding `🚀` before each heading.
